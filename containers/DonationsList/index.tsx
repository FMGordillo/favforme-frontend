import { FunctionComponent } from "react";
import { Layout } from "@/components";

export const DonationsList: FunctionComponent = () => {
  return (
    <Layout header headerProps={{ title: "Donaciones" }} footer>
      <p>TODO</p>
    </Layout>
  );
};
